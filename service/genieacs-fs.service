[Unit]
Description=GenieACS FS
After=network.target mongodb.service

[Service]
User=genieacs
EnvironmentFile=/opt/genieacs/config.env
ExecStart=/usr/bin/genieacs-fs
Restart=always

[Install]
WantedBy=multi-user.target
